# Folding as a generative model {#generative}

*How do you turn one pattern into a family of test problems?*

<!-- TODO: chapter content. Outline below is the plan of record. -->

- The Miura-ori parameterisation $(a, b, \alpha, \theta)$.
- Uniform sampling on facets, and why naive vertex sampling biases the density.
- Noise models: ambient Gaussian, on-manifold, and outliers.
- What $\theta$ does to the $k$-nearest-neighbour graph, and where short-circuits appear.
- Why every result downstream is a curve over $\theta$ rather than a single number.
